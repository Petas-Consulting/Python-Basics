#A statement is an instruction that a Python interpreter can execute. So, in simple words, we can say anything written
#in Python is a statement.Python statement ends with the token NEWLINE character. It means each line in a Python script
#is a statement

#Expressions are representations of value. They are different from statement in the fact that statements do something
#while expressions are representation of value. For example any string is also an expressions since it represents the
#value of the string as well.



print(5 + 4)
print(len("Python")) # len with print out the total number of character present in Python



a = len("Hello")
b = len("Python")
print(a + b)

print(len("Hello") + len("Python"))

