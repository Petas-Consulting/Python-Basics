num = [1,2,3,4,5,6,7,8,9]
accm = 0  # we set the accumulator to zero
for n in num:
    accm = accm + n # set another value for our accumulator. now it wil make the sum of all the value we have in our list 'num'
print(accm)
