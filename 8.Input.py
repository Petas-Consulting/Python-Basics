# We can also ask theuser to enter the data they wish to input so we do use the input function

name = (input("Enter your name: "))
print(name)

# We can also declare the type of datatype we want theuser to input
age = int(input("Your age: "))  # this will only the use to put in a integer value if the user inputted a wrong datatype value an error will occur
print(age)
